# V2: форматирование шагов формы

Тексты всех шагов анкеты (мастер заполнения проекта) проходят через единый форматтер в `src/v2/format_step.py` и отправляются с `parse_mode="HTML"`.

**Стиль сообщения:** сверху строка «📌 Шаг X из N», затем пустая строка; заголовок шага — жирным; уточнение в скобках (например, «одной строкой») — отдельной строкой; основной текст и подсказки разбиты на абзацы с пустой строкой между блоками; блоки «Пример:» / «Например:» / «Примеры:» выводятся с пустой строкой перед ними и курсивной подписью. Списки нормализуются к буллетам «•». Эмодзи не изменяются.

Использование: все места, где показывается вопрос шага (первый показ, «Назад», «Дальше», повтор при ошибке валидации, resume), вызывают `show_question()` в `src/v2/routers/form.py`, который строит текст через `_question_text()` и форматтер. Менять разметку шагов нужно в копирайте (`src/bot/messages.py`, ключи `V2_FORM_*`, `V2_PROJECT_*`, и т.д.); единый вид обеспечивает форматтер.
