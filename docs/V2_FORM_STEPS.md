# V2: форматирование шагов формы и меню-кабинет

## Единый шаблон шагов

Тексты всех шагов анкеты проходят через централизованную функцию `format_step_message()` в `src/v2/format_step.py` и отправляются с `parse_mode="HTML"`.

**Формат сообщения шага:**
- Первая строка: «Шаг X из Y»
- Пустая строка
- Блок «📌 *Название шага*» (жирный заголовок)
- Пустая строка
- Короткое пояснение (1–2 предложения), если есть
- При наличии — блок «📝 *Что нужно сделать:*» и нумерованный список
- Пустая строка
- *Пример:* «…» (курсив)

Между смысловыми блоками — пустые строки. Исходные тексты берутся из копирайта (`messages.py`, ключи `V2_FORM_*`, `V2_PROJECT_*` и т.д.) и разбираются через `parse_copy_to_parts()` (первая строка — заголовок, далее пояснение, строка «Пример:» — пример).

Использование: все места, где показывается вопрос шага (первый показ, «Назад», повтор при ошибке, resume), вызывают `show_question()` в `src/v2/routers/form.py`, который строит текст через `_question_text()` → `format_step_message()` + `parse_copy_to_parts()`.

## Меню-кабинет

Кнопка **«☰ Меню»** (Reply-клавиатура) показывается при входе в V2 и при создании/продолжении формы; с любого шага пользователь может нажать «Меню» и попасть в кабинет.

**Кабинет:** в сообщении отображаются текущий проект (название или «Проект не задан»), текущий шаг «X из Y» и прогресс в процентах. Кнопки: Продолжить, Текущий шаг (показать текст текущего шага), Проект (резюме собранных данных или заглушка), Начать заново (с подтверждением Да/Нет), Мои проекты, Создать проект, Помощь. Команды бота (/start, /help, /resume и др.) описаны в разделе «Помощь».
